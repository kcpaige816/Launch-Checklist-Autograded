 // let formInput = document.querySelectorAll("form");
    // let validationObject = {};

    // use validateInput() somehow instead
    // for (let i = 0; i < formInput.length; i++) { 
    //         validationObject = formInput[i];
    //         if (validationObject[i] === "") {
    //         return "Invalid. Missing input. "
    //     } else {
            
    //     }
    // }

    // ^^trying to put form input values into an array, loop thru array items w/ validateInput(), 
    // and then return statement. or object with key/value pairs?